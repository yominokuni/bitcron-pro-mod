<!DOCTYPE html>
<html>
<head>

    <!--    点击爆炸特效 start-->
    <canvas class="fireworks" style="position:fixed;left:0;top:0;z-index:99999999;pointer-events:none;"></canvas>
    <script src="https://a-oss.zmki.cn/20190502/baozatexiao.js"></script>
    <!--    点击爆炸特效 ends-->
	<%- include('./_blocks/head', { siteTitle: `friends | ${themeConfig.siteName}` }) %>

</head>
<body>

<%- include('./_blocks/header') %>
<div class="main"><div class="main-inner"><div class="content">
<article class="post">
<h2 class="post_title">友情链接</h2>
<div style="display:flex">
<div class="md_block" id="md_block">

<blockquote>
<ul>
<li>名称：一片痴心俱成灰</li>
<li>说明：跳跃式移动，电波系发言。</li>
<li>地址：<a href="https://akaito.xyz/">https://akaito.xyz/</a></li>
<li>图标：<a href="https://akaito.xyz/images/avatar.png">https://akaito.xyz/images/avatar.png</a></li>
</ul>
</blockquote>

<div class="round-shape-one"></div>

<div class="post-content yue flex flex-wrap">
         
          <% if (site.customConfig.friends) { %>
            <% site.customConfig.friends.forEach(function(friend) { %>
              <div class="friend-box">
                <a href="<%= friend.siteLink %>" target="_blank"><img class="friend-avatar" src="<%= friend.siteLogo %>"></a>
                <div class="flink-info">
                  <a href="<%= friend.siteLink %>" target="_blank"><%= friend.siteName %></a>
                  <br/>
                  <%= friend.description %>
                </div>
              </div>
            <% }); %>

            <script src="https://cdn.staticfile.org/jquery/1.10.2/jquery.min.js"></script>
            <script>
              // 这里注入了一个Fisher–Yates Shuffle算法用来洗牌
              jQuery.prototype.shuffle = function() {
                var res = this;
                for (var i = res.length - 1; i >= 0; i--) {
                  var rnd = Math.floor(Math.random() * (i + 1));
                  var ind = res[rnd];

                  res[rnd] = res[i];
                  res[i] = ind;
                }
                return res;
              }
              // 打乱一下顺序
              var items = $(".friend-box").shuffle();
              $(".post-content").empty().append(items);
          </script>
          <% } %>

        </div>

<span id="footnote"></span>
<div id = "warn"></div>
</div>

</div>
<div id="fullPage"><canvas id="canvas"></canvas></div>
</article>
<div id="eof"><span>EOF</span></div><div class="round-shape-one"></div>
<section>
<div class="doc_comments">
<%- include('./_blocks/waline') %>
<% if (typeof commentSetting !== 'undefined' && commentSetting.showComment) { %>
	  <% if (commentSetting.commentPlatform === 'gitalk') { %>
		<div id="gitalk-container"></div>
		<%- include('./_blocks/gitalk') %>
	  <% } %>
		  
	  <% if (commentSetting.commentPlatform === 'disqus') { %>
		<%- include('./_blocks/disqus') %>
	  <% } %>
<% } %>
</div></section>
</div></div></div><script>
"use strict";!function(){for(var n=document.getElementsByTagName("pre"),e=n.length,s=0;s<e;s++){n[s].innerHTML='<span class="line-number"></span>'+n[s].innerHTML+'<span class="cl"></span>';for(var a=n[s].innerHTML.split(/\n/).length,r=0;r<a-1;r++){n[s].getElementsByTagName("span")[0].innerHTML+="<span>"+(r+1)+"</span>"}}}();
let mainNavLinks=document.querySelectorAll(".markdownIt-TOC a");window.addEventListener("scroll",event=>{let fromTop=window.scrollY;mainNavLinks.forEach((link,index)=>{let section=document.getElementById(decodeURI(link.hash).substring(1));let nextSection=null
if(mainNavLinks[index+1]){nextSection=document.getElementById(decodeURI(mainNavLinks[index+1].hash).substring(1));}
if(section.offsetTop<=fromTop){if(nextSection){if(nextSection.offsetTop>fromTop){link.classList.add("currentToc");}else{link.classList.remove("currentToc");}}else{link.classList.add("currentToc");}}else{link.classList.remove("currentToc");}});});
var h=document.documentElement,b=document.body,st="scrollTop",sh="scrollHeight",progress=document.querySelector(".progress"),scroll;document.addEventListener("scroll",function(){scroll=(h[st]||b[st])/((h[sh]||b[sh])-h.clientHeight)*100;progress.style.setProperty("--scroll",scroll+"%")});

</script>


<%- include('./_blocks/footer') %>
<script type="text/javascript" async src="<%= themeConfig.domain %>/media/js/prism.js"></script>
</body>
</html>